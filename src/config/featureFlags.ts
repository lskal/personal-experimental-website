// Shared across the Hero contact panel and the Contact/footer section —
// keep here rather than duplicating the constant in both files.
export const SHOW_LOCATION = true;
export const SHOW_EMAIL = true;
export const SHOW_PHONE = true;
export const SHOW_GITHUB = true;
export const SHOW_LINKEDIN = true;
export const SHOW_MBTI = true;


// Temporary reference block for the color-palette decision (CLAUDE.md backlog item 1).
// Flip to true to show it again; set back to false once a palette is chosen.
export const SHOW_PALETTE_DEMO = false;
